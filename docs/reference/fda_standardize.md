# Standardization Preprocessing Spec

Standardization Preprocessing Spec

## Usage

``` r
fda_standardize(center = TRUE, scale = TRUE)
```

## Arguments

- center, scale:

  Logical flags controlling column-wise centering and scaling. Both
  default to `TRUE`.

## Value

An object of class `fda_preprocess_spec`.
