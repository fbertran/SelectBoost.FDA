% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{plain_selectboost}
\alias{plain_selectboost}
\title{Plain SelectBoost Baseline for Functional Predictors}
\usage{
plain_selectboost(
  x,
  y = NULL,
  mode = c("fast", "auto"),
  selector = "msgps",
  selector_fun = NULL,
  selector_args = list(),
  groups = NULL,
  family = c("gaussian", "binomial"),
  association = NULL,
  group_method = c("threshold", "community"),
  ...
)
}
\arguments{
\item{x}{Any input accepted by \code{as_functional_matrix()}, or an \code{fda_design}
object.}

\item{y}{Response vector. Leave as \code{NULL} when \code{x} is an \code{fda_design}.}

\item{mode}{\code{"fast"} for a fixed \code{c0} grid or \code{"auto"} for the adaptive
version.}

\item{selector}{Base selector used inside SelectBoost. Choose from
\code{"msgps"}, \code{"lasso"}, \code{"group_lasso"}, \code{"sparse_group_lasso"}, the
backend-specific aliases \code{"glmnet"}, \code{"grpreg"}, \code{"sgl"}, or provide a
custom function.}

\item{selector_fun}{Optional custom base selector. It must return a
coefficient vector of length \code{p}.}

\item{selector_args}{Optional named list of arguments forwarded to the base
selector.}

\item{groups}{Optional feature groups used by grouped base selectors such as
\code{"grpreg"}. Defaults to block-level groups for list inputs.}

\item{family}{Model family passed to built-in selectors.}

\item{association}{Optional absolute association matrix used directly by the
raw SelectBoost grouping function.}

\item{group_method}{Functional grouping backend: threshold-based or
community-based.}

\item{...}{Additional arguments passed to \code{SelectBoost::fastboost()} or
\code{SelectBoost::autoboost()}.}
}
\value{
An object of class \code{plain_selectboost_result}.
}
\description{
Runs \code{SelectBoost} directly on the flattened predictor matrix without the
FDA-specific grouping heuristics used by \code{\link[=selectboost_fda]{selectboost_fda()}}. This is useful
as a benchmark against the FDA-aware variant.
}
