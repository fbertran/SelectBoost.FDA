% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-selection.R
\name{stability_selection_fda}
\alias{stability_selection_fda}
\title{Grouped Stability Selection for Functional Predictors}
\usage{
stability_selection_fda(
  x,
  y = NULL,
  selector = c("grpreg", "glmnet"),
  selector_fun = NULL,
  groups = NULL,
  family = c("gaussian", "binomial"),
  B = 100L,
  sample_fraction = 0.5,
  cutoff = 0.75,
  seed = NULL,
  keep_subsamples = FALSE,
  ...
)
}
\arguments{
\item{x}{Any input accepted by \code{as_functional_matrix()}, or an \code{fda_design}
object.}

\item{y}{Response vector. Leave as \code{NULL} when \code{x} is an \code{fda_design}.}

\item{selector}{Either \code{"glmnet"}, \code{"grpreg"}, or a custom function.}

\item{selector_fun}{Optional custom selector. It must accept \code{X}, \code{y},
\code{groups}, and \code{family}, and return either a coefficient vector or a logical
selection vector of length \code{p}.}

\item{groups}{Optional grouping structure. Defaults to block-level groups when
\code{x} is supplied as a list, and otherwise to one group per feature.}

\item{family}{Model family passed to the built-in selectors.}

\item{B}{Number of subsampling replicates.}

\item{sample_fraction}{Fraction of observations drawn without replacement in
each subsample.}

\item{cutoff}{Stability threshold used to define \code{selected_features} and
\code{selected_groups}.}

\item{seed}{Optional random seed.}

\item{keep_subsamples}{Should the sampled row indices be returned?}

\item{...}{Additional arguments forwarded to the built-in or custom selector.}
}
\value{
An object of class \code{fda_stability_selection}.
}
\description{
Repeatedly subsamples observations, refits a sparse base selector, and
computes exact feature- and group-level selection frequencies. This is the
generic FDA recipe for basis expansions, discretized curves, or FPCA scores.
}
