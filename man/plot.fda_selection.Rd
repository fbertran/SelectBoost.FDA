% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.fda_selection}
\alias{plot.fda_selection}
\alias{plot.fda_stability_selection}
\alias{plot.selectboost_fda_result}
\title{Plot FDA Selection Results}
\usage{
\method{plot}{fda_stability_selection}(
  x,
  type = c("feature", "group", "interval", "basis"),
  value = c("group", "mean", "max"),
  facet = c("none", "predictor"),
  palette = selection_palette(),
  show_legend = TRUE,
  legend_title = NULL,
  legend_n_ticks = 3L,
  legend_digits = 2L,
  legend_cex = 0.75,
  cutoff = x$cutoff,
  ...
)

\method{plot}{selectboost_fda_result}(
  x,
  type = c("feature", "group", "basis"),
  value = c("max", "mean"),
  palette = selection_palette(),
  show_legend = TRUE,
  legend_title = NULL,
  legend_n_ticks = 3L,
  legend_digits = 2L,
  legend_cex = 0.75,
  c0 = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=stability_selection_fda]{stability_selection_fda()}},
\code{\link[=interval_stability_selection]{interval_stability_selection()}}, \code{\link[=fit_stability]{fit_stability()}}, \code{\link[=selectboost_fda]{selectboost_fda()}},
or \code{\link[=fit_selectboost]{fit_selectboost()}}.}

\item{type}{Summary view to plot. Stability-selection fits support
\code{"feature"}, \code{"group"}, \code{"interval"}, and \code{"basis"}. SelectBoost fits
support \code{"feature"}, \code{"group"}, and \code{"basis"}.}

\item{value}{Quantity summarized in group, interval, and basis views.
Stability-selection fits accept \code{"group"}, \code{"mean"}, and \code{"max"}.
SelectBoost fits accept \code{"mean"} and \code{"max"}.}

\item{facet}{Faceting mode for interval heatmaps. Currently only
\code{type = "interval"} uses this argument.}

\item{palette}{Vector of colors used for heatmaps.}

\item{show_legend}{Logical; should heatmap views draw a legend?}

\item{legend_title}{Optional legend title for heatmap views. By default an
informative title is chosen from \code{type} and \code{value}.}

\item{legend_n_ticks}{Approximate number of tick marks used in the heatmap
legend.}

\item{legend_digits}{Number of significant digits used for heatmap legend
labels.}

\item{legend_cex}{Character expansion used for heatmap legend text.}

\item{cutoff}{Stability threshold. Only used for \code{fda_stability_selection}
objects.}

\item{...}{Additional graphical parameters passed to bar-plot-based views.}

\item{c0}{Optional SelectBoost correlation threshold. When omitted,
SelectBoost heatmaps are drawn across all available \code{c0} values.}
}
\value{
Invisibly returns the helper output used to build the plot.
}
\description{
Plots feature-, group-, interval-, and basis-level summaries derived from
\code{\link[=selection_map]{selection_map()}}. The available views depend on the fitted object:
}
\details{
\itemize{
\item \code{fda_stability_selection} supports \code{type = "feature"}, \code{"group"},
\code{"interval"}, and \code{"basis"}.
\item \code{selectboost_fda_result} supports \code{type = "feature"}, \code{"group"}, and
\code{"basis"}.
}

Heatmap-based views are used for interval summaries and for SelectBoost
summaries over multiple \code{c0} values. Bar-plot views are used otherwise.
}
\seealso{
\code{\link[=selection_map]{selection_map()}}
}
