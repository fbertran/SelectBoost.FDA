% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbation-grid.R
\name{fit_perturbation_grid}
\alias{fit_perturbation_grid}
\title{Fit a Two-Parameter FDA Perturbation Grid}
\usage{
fit_perturbation_grid(
  x,
  y = NULL,
  q_grid = c(0.5, 0.632, 0.8),
  c0_grid = seq(0.1, 0.9, by = 0.1),
  B = 100L,
  selectboost_B = 1L,
  selector = "group_lasso",
  selector_fun = NULL,
  selector_args = list(),
  family = c("gaussian", "binomial"),
  association_method = c("correlation", "neighborhood", "hybrid", "interval"),
  group_method = c("threshold", "community"),
  within_blocks = TRUE,
  bandwidth = NULL,
  width = NULL,
  step = width,
  levels = c("feature", "group", "basis"),
  cutoff = 0,
  seed = NULL,
  n_cores = 1L,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{x}{Any input accepted by \code{\link[=as_functional_matrix]{as_functional_matrix()}}, or an
\code{fda_design}.}

\item{y}{Response vector. Leave as \code{NULL} when \code{x} is an \code{fda_design}.}

\item{q_grid}{Subsampling fractions.}

\item{c0_grid}{SelectBoost \code{c0} values.}

\item{B}{Number of row-subsampling replicates.}

\item{selectboost_B}{Number of internal SelectBoost perturbation replicates
per subsample.}

\item{selector, selector_fun, selector_args, family, association_method, group_method}{Base selector and FDA-aware grouping arguments passed to
\code{\link[=selectboost_fda]{selectboost_fda()}}.}

\item{within_blocks, bandwidth, width, step}{Functional association and
interval-structure arguments passed to \code{\link[=selectboost_fda]{selectboost_fda()}}.}

\item{levels}{Selection levels to store in the surface.}

\item{cutoff}{Selection cutoff used for the \code{selected} column.}

\item{seed}{Optional seed used in a local RNG scope.}

\item{n_cores}{Reserved for future parallel backends. The current
implementation runs serially.}

\item{keep_fits}{Should individual fitted objects be retained?}

\item{...}{Additional arguments passed to \code{\link[=selectboost_fda]{selectboost_fda()}}.}
}
\value{
An object of class \code{fda_perturbation_grid}.
}
\description{
Runs FDA-aware \code{SelectBoost} over a grid of subject subsampling rates and
\code{c0} perturbation strengths, returning a renderer-neutral selection surface.
}
